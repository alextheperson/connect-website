{
  "scripts": {
    "dev": "tsc; node app.js",
    "build": "tsc; mkdir dist; cp -a engines dist; cp -a public dist; cp -a src dist; cp app.js dist; cp package.json dist; cp .htaccess dist"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "socket.io": "^4.7.1"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/multer": "^1.4.12",
    "@types/node": "^7.10.14",
    "typescript": "^5.1.6"
  }
}
